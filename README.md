# automated-meta-ads-toolkit

A clean, reusable CLI harness for managing Meta Ads across multiple clients.

## What is this?

This toolkit makes it easy to run Meta Ads commands while keeping each client's credentials separate and secure. It is designed so that other companies can duplicate this repo and quickly customize it for their own use.

## Quick Start

```bash
# Run commands against a specific client
meta client-example campaigns list
